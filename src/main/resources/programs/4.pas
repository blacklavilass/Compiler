var a1 : String;
    b1, c1 : integer;

function f(a : integer; b : string): integer;
begin
result := 1
end;

begin
  b1 := f(c1, a1);
  while b1 > 0 do
    begin
        Writeln(b1);
        var h1 := b1 - 1;
    end;
end.